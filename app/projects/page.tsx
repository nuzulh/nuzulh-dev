import { PageContainer } from "@/components/container";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Projects | Nuzul H."
};

export default function ProjectsPage() {
  return (
    <PageContainer>
      <main>
        <i>in the making...</i>
      </main>
    </PageContainer>
  );
}
